{
  "configurations": [
    {
      "type": "pwa-chrome",
      "name": "localhost:3000",
      "request": "launch",
      "url": "http://localhost:3000",
			"preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
